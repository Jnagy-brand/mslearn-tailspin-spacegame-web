# ASP.NET Core

trigger:
- main

pool:
  name: 'Azure Pipelines'

variables:
  buildConfiguration: 'Release'

steps:
- script: dotnet build --configure $(buildConfiguration)
  displayName: 'dotnet build $(buildConfiguration)'

# ASP.NET Core

trigger:
- main

pool:
  vmImage: Default

variables:
  buildConfiguration: 'Release'

steps:
- script: dotnet build --configure $(buildConfigration)
  displayName: 'dotnet build $(buildConfiguration)'
